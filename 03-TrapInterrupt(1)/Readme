# 第3章: XOS with Trap and Interrupt Handling

## 目录结构

- `Makefile`: 构建规则和编译配置。
- `src/main.c`: 主要的C源代码文件，包含内核的初始化和中断测试。
- `src/print.c`: 实现打印功能的模块。
- `src/sbi.c`: 实现RISC-V SBI调用的模块。
- `src/trap.c`: 实现中断和异常处理的模块。
- `kernel.S`: 汇编代码，包含内核的入口点。
- `trapentry.S`: 汇编代码，包含中断和异常的处理入口。
- `linker.ld`: 链接脚本，定义内存布局和段分配。
- `README.md`: 项目说明文档。

## 编译

1. 确保安装了 RISC-V 工具链。
2. 运行 `make` 命令编译项目。

## 运行

编译完成后，生成的 `kernel.elf` 可以在 RISC-V 模拟器或硬件平台上加载和运行。

## 新增内容

这一章相较于前一章的新增内容：

### 1. 引入中断和异常处理机制

在本章中，我们引入了中断和异常处理机制，使内核能够响应各种硬件和软件事件。具体实现包括：
- `trap.c`：实现了中断和异常处理函数，如 `handle_interrupt` 和 `handle_exception`。
- `trapentry.S`：实现了中断和异常的处理入口，确保这些事件能够被正确捕获和处理。

### 2. 修改主程序 (`main.c`)

在 `main.c` 中，我们初始化了中断处理，并进行了简单的中断测试。例如：

```c
init_interrupt();
test_interrupts();

### 3. 学习目标

通过本章的学习，学生将掌握以下知识点：

中断和异常处理的基本概念。
如何在RISC-V内核中实现中断和异常处理机制。
使用汇编代码实现中断处理入口。

### 4. 下一步

在接下来的章节中，我们将进一步扩展内核的功能，引入物理内存管理机制，使内核能够管理和分配物理内存。